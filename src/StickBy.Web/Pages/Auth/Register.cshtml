@page
@model RegisterModel
@inject ILocalizationService L
@{
    ViewData["Title"] = L["auth.register"];
}

<div class="auth-container">
    <h1>@L["auth.register"]</h1>

    @if (!string.IsNullOrEmpty(Model.ErrorMessage))
    {
        <div class="error-message">@Model.ErrorMessage</div>
    }

    <form method="post">
        <div class="form-group">
            <label asp-for="DisplayName">@L["auth.displayName"]</label>
            <input asp-for="DisplayName" type="text" class="form-control" required />
        </div>

        <div class="form-group">
            <label asp-for="Email">@L["auth.email"]</label>
            <input asp-for="Email" type="email" class="form-control" required />
        </div>

        <div class="form-group">
            <label asp-for="Password">@L["auth.password"]</label>
            <input asp-for="Password" type="password" class="form-control" required />
            <small>Mindestens 8 Zeichen, Groß- und Kleinbuchstaben, Zahl</small>
        </div>

        <button type="submit" class="btn-primary" style="width: 100%;">@L["auth.register"]</button>
    </form>

    <div class="auth-link">
        <p>@L["auth.hasAccount"] <a href="/Auth/Login">@L["nav.login"]</a></p>
    </div>
</div>
